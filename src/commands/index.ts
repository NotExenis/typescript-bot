import * as ping from "./ping";
import * as kick from "./kick";
import * as unban from "./unban";   
import * as ban from "./ban";
import * as logSetup from "./log_setup";

export const commands = {
    ping,
    kick,
    unban,
    ban,
    logSetup,
}